{
  "name": "@workspace/widget",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "mkdir -p ../../apps/web/public && esbuild src/index.ts --bundle --minify --outfile=../../apps/web/public/widget.js --platform=browser --target=es2017",
    "dev": "mkdir -p ../../apps/web/public && esbuild src/index.ts --bundle --outfile=../../apps/web/public/widget.js --platform=browser --target=es2017 --watch"
  },
  "devDependencies": {
    "@workspace/typescript-config": "workspace:*",
    "esbuild": "^0.25.0",
    "typescript": "^5.9.3"
  }
}
